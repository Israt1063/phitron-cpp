# Define a function to add two numbers
def add_numbers(num1, num2):  # Renamed 'sum' to 'add_numbers' to avoid conflict
    result = num1 + num2
    print(result)

add_numbers(10, 4)

# Define a function to double a number
def double_it(num):  # Fixed typo from 'dobule_it' to 'double_it'
    return num * 2

total = double_it(3)
print(total)
